version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: talent_bank_redis_dev
    restart: always
    ports:
      - "${REDIS_PORT:-6379}:6379"
    command: redis-server --requirepass ${REDIS_PASSWORD:-1234}
    volumes:
      - redis_data:/data

volumes:
  redis_data: {}
